# backup.config - sample configuration for backup.sh
# Put this next to backup.sh or pass --config /path/to/backup.config

# Where to store backups (expand ~ if necessary)
BACKUP_DESTINATION="/mnt/c/Users/hp/Documents/bash_projects/backups"


# Comma-separated list of patterns to exclude (passed to tar --exclude)
EXCLUDE_PATTERNS=".git,node_modules,.cache,.DS_Store"

# Rotation policy
RETENTION_COUNT=3
DAILY_KEEP=7
WEEKLY_KEEP=4
MONTHLY_KEEP=3


# Email for notifications (script writes an email.txt to BACKUP_DESTINATION to simulate)
EMAIL_NOTIFY="spk@example.com"
